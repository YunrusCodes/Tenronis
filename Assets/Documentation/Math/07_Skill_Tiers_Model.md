# 07 - 技能層級模型
# Skill Tiers Model

**文檔版本**: 2.1  
**最後更新**: 2025年12月1日  
**新增內容**: 三層玩家能力模型與平衡匹配分析

---

## 📋 目錄

1. [技能層級定義](#技能層級定義)
2. [分層傷害公式](#分層傷害公式)
3. [分層技能效率](#分層技能效率)
4. [分層反擊系統](#分層反擊系統)
5. [分層版面管理](#分層版面管理)
6. [火力曲線匹配分析](#火力曲線匹配分析)

---

## 🎯 技能層級定義

### 三層玩家分類

#### 層級1：普通玩家 (Casual)
```
技能等級：S_level ∈ [0, 35]
Combo範圍：C ∈ [3, 6]
操作特徵：
- 低放置頻率（0.3-0.4 個/秒）
- 多單行消除
- Combo難以維持
- 反應較慢（T_react > 0.4秒）
- 失誤率較高（5-8%）
```

**關鍵參數**：
```
λ_pieces_casual = 0.35 個/秒
E[r]_casual = 1.8 行
E[C]_casual = 4.5
P_counter_casual = 0.15（反擊成功率15%）
P_repair_casual = 0.60（修補效果60%）
```

#### 層級2：中階玩家 (Intermediate)
```
技能等級：S_level ∈ (35, 65]
Combo範圍：C ∈ [8, 12]
操作特徵：
- 中等放置頻率（0.45-0.55 個/秒）
- 多2-3行消除
- Combo可穩定維持
- 反應適中（T_react ≈ 0.35秒）
- 失誤率中等（3-5%）
```

**關鍵參數**：
```
λ_pieces_inter = 0.50 個/秒
E[r]_inter = 2.5 行
E[C]_inter = 10
P_counter_inter = 0.40（反擊成功率40%）
P_repair_inter = 0.75（修補效果75%）
```

#### 層級3：高手玩家 (Expert)
```
技能等級：S_level ∈ (65, 100]
Combo範圍：C ∈ [20, 40]
操作特徵：
- 高放置頻率（0.6-0.8 個/秒）
- 多3-4行消除
- Combo長期維持
- 反應快速（T_react < 0.30秒）
- 失誤率低（1-3%）
```

**關鍵參數**：
```
λ_pieces_expert = 0.70 個/秒
E[r]_expert = 3.2 行
E[C]_expert = 30
P_counter_expert = 0.75（反擊成功率75%）
P_repair_expert = 0.90（修補效果90%）
```

### 層級參數映射函數

```
Tier(S_level) = {
    Casual,       if S_level ∈ [0, 35]
    Intermediate, if S_level ∈ (35, 65]
    Expert,       if S_level ∈ (65, 100]
}

參數映射：
λ_pieces(tier) ∈ {0.35, 0.50, 0.70}
E[r](tier) ∈ {1.8, 2.5, 3.2}
E[C](tier) ∈ {4.5, 10, 30}
P_counter(tier) ∈ {0.15, 0.40, 0.75}
P_repair(tier) ∈ {0.60, 0.75, 0.90}
```

---

## 💥 分層傷害公式

### 單發導彈傷害（分層）

#### 完整分層公式

```
DMG_single(r, tier, L_salvo, L_burst) = D_base + B_salvo(r, L_salvo) + B_burst(tier, L_burst)

其中：
D_base = 2.0
B_salvo(r, L_salvo) = (r-1) × L_salvo × 0.5
B_burst(tier, L_burst) = E[C](tier) × L_burst × 0.25
```

**分層展開**：
```
B_burst_casual = 4.5 × L_burst × 0.25 = 1.125 × L_burst
B_burst_inter = 10 × L_burst × 0.25 = 2.5 × L_burst
B_burst_expert = 30 × L_burst × 0.25 = 7.5 × L_burst
```

### 總傷害輸出（分層）

#### 基礎公式

```
DMG_total(r, tier, L_volley, L_salvo, L_burst) = N_missiles(r, L_volley) × DMG_single(r, tier, L_salvo, L_burst)
```

#### 實際計算範例

**Stage 1（初期，所有等級Lv1）**

```
普通玩家：
r = 1.8（平均）, C = 4.5, L_volley = 0, L_salvo = 1, L_burst = 1
DMG_single = 2.0 + 0×1×0.5 + 4.5×1×0.25 = 2.0 + 0 + 1.125 = 3.125
N_missiles = 1.8 × 10 × 1 = 18
DMG_total = 18 × 3.125 = 56.25

中階玩家：
r = 2.5, C = 10
DMG_single = 2.0 + (2.5-1)×1×0.5 + 10×1×0.25 = 2.0 + 0.75 + 2.5 = 5.25
N_missiles = 2.5 × 10 × 1 = 25
DMG_total = 25 × 5.25 = 131.25

高手玩家：
r = 3.2, C = 30
DMG_single = 2.0 + (3.2-1)×1×0.5 + 30×1×0.25 = 2.0 + 1.1 + 7.5 = 10.6
N_missiles = 3.2 × 10 × 1 = 32
DMG_total = 32 × 10.6 = 339.2
```

**倍數差異**（Stage 1）：
```
高手/普通 = 339.2 / 56.25 = 6.03倍
高手/中階 = 339.2 / 131.25 = 2.58倍
```

**Stage 10（中期，Volley=2, Salvo=3, Burst=3）**

```
普通玩家：
DMG_single = 2.0 + (1.8-1)×3×0.5 + 4.5×3×0.25 = 2.0 + 1.2 + 3.375 = 6.575
N_missiles = 1.8 × 10 × 3 = 54
DMG_total = 54 × 6.575 = 355

中階玩家：
DMG_single = 2.0 + (2.5-1)×3×0.5 + 10×3×0.25 = 2.0 + 2.25 + 7.5 = 11.75
N_missiles = 2.5 × 10 × 3 = 75
DMG_total = 75 × 11.75 = 881

高手玩家：
DMG_single = 2.0 + (3.2-1)×3×0.5 + 30×3×0.25 = 2.0 + 3.3 + 22.5 = 27.8
N_missiles = 3.2 × 10 × 3 = 96
DMG_total = 96 × 27.8 = 2,669
```

**倍數差異**（Stage 10）：
```
高手/普通 = 2669 / 355 = 7.52倍
高手/中階 = 2669 / 881 = 3.03倍
```

**Stage 20（後期，Volley=5, Salvo=6, Burst=6）**

```
普通玩家：
DMG_single = 2.0 + (1.8-1)×6×0.5 + 4.5×6×0.25 = 2.0 + 2.4 + 6.75 = 11.15
N_missiles = 1.8 × 10 × 6 = 108
DMG_total = 108 × 11.15 = 1,204

中階玩家：
DMG_single = 2.0 + (2.5-1)×6×0.5 + 10×6×0.25 = 2.0 + 4.5 + 15.0 = 21.5
N_missiles = 2.5 × 10 × 6 = 150
DMG_total = 150 × 21.5 = 3,225

高手玩家：
DMG_single = 2.0 + (3.2-1)×6×0.5 + 30×6×0.25 = 2.0 + 6.6 + 45.0 = 53.6
N_missiles = 3.2 × 10 × 6 = 192
DMG_total = 192 × 53.6 = 10,291
```

**倍數差異**（Stage 20）：
```
高手/普通 = 10291 / 1204 = 8.55倍
高手/中階 = 10291 / 3225 = 3.19倍
```

### 傷害增長分析

| Stage | 普通玩家 | 中階玩家 | 高手玩家 | 普通增長 | 中階增長 | 高手增長 |
|-------|---------|---------|---------|---------|---------|---------|
| 1 | 56 | 131 | 339 | 1× | 1× | 1× |
| 5 | 250 | 600 | 1,800 | 4.5× | 4.6× | 5.3× |
| 10 | 355 | 881 | 2,669 | 6.3× | 6.7× | 7.9× |
| 15 | 680 | 1,900 | 6,200 | 12.1× | 14.5× | 18.3× |
| 20 | 1,204 | 3,225 | 10,291 | 21.4× | 24.6× | 30.3× |

**結論**：
- 高手玩家的傷害增長速度最快（30.3倍）
- 普通玩家的傷害增長最慢（21.4倍）
- 層級差距隨關卡推進而擴大

---

## 🔧 分層技能效率

### 處決技能（Execution）

#### 分層效率公式

```
DMG_exec(tier, L_volley) = n_active(tier) × (1 + L_volley) × 4.0

其中：
n_active(tier) = 清除的方塊數量（受操作準確度影響）
```

**分層清除率**：
```
n_active_casual = W × 0.65 = 6.5 個（65%準確度）
n_active_inter = W × 0.85 = 8.5 個（85%準確度）
n_active_expert = W × 0.95 = 9.5 個（95%準確度）
```

**實際傷害計算**（Volley=3）：

```
普通玩家：
DMG_exec = 6.5 × (1+3) × 4.0 = 6.5 × 4 × 4 = 104
效率 = 104 / 5 = 20.8 傷害/CP

中階玩家：
DMG_exec = 8.5 × (1+3) × 4.0 = 8.5 × 4 × 4 = 136
效率 = 136 / 5 = 27.2 傷害/CP

高手玩家：
DMG_exec = 9.5 × (1+3) × 4.0 = 9.5 × 4 × 4 = 152
效率 = 152 / 5 = 30.4 傷害/CP
```

**效率比較**：
```
高手/普通 = 30.4 / 20.8 = 1.46倍
高手/中階 = 30.4 / 27.2 = 1.12倍
```

### 修補技能（Repair）

#### 分層效果模型

```
Effect_repair(tier, n_holes) = Fill_success(tier, n_holes) + Trigger_bonus(tier)

其中：
Fill_success = n_holes × P_repair(tier)
Trigger_bonus = 觸發消除的額外傷害（依賴填補質量）
```

**分層填補成功率**：
```
P_repair_casual = 0.60（60%空洞被有效填補）
P_repair_inter = 0.75（75%）
P_repair_expert = 0.90（90%）
```

**實際效果計算**（10個空洞）：

```
普通玩家：
有效填補 = 10 × 0.60 = 6個
觸發消除概率 = 0.30（30%）
E[Effect] = 6 + 0.30 × E[DMG_total_casual]
          ≈ 6 + 0.30 × 100 = 36單位效果

中階玩家：
有效填補 = 10 × 0.75 = 7.5個
觸發消除概率 = 0.50（50%）
E[Effect] = 7.5 + 0.50 × E[DMG_total_inter]
          ≈ 7.5 + 0.50 × 300 = 158單位效果

高手玩家：
有效填補 = 10 × 0.90 = 9個
觸發消除概率 = 0.70（70%）
E[Effect] = 9 + 0.70 × E[DMG_total_expert]
          ≈ 9 + 0.70 × 800 = 569單位效果
```

**CP效率**：
```
普通玩家：36 / 30 = 1.2 效果/CP
中階玩家：158 / 30 = 5.3 效果/CP
高手玩家：569 / 30 = 19.0 效果/CP

高手/普通 = 15.8倍（技能使用差距極大）
```

---

## ⚔️ 分層反擊系統

### 反擊成功率模型

#### 時間窗口內成功率

```
P_counter(tier) = P_base(tier) × f_attention(tier) × f_reaction(tier)

其中：
P_base: 基礎成功率（技能依賴）
f_attention: 注意力因子（認知負載）
f_reaction: 反應速度因子
```

**分層基礎成功率**：
```
P_base_casual = 0.15（15%基礎成功率）
P_base_inter = 0.40（40%）
P_base_expert = 0.75（75%）
```

**認知負載影響**：
```
f_attention(tier, n) = 1 / (1 + α_tier × CT(n))

其中：
α_casual = 0.08（高認知負載影響）
α_inter = 0.04（中等影響）
α_expert = 0.02（低影響）
```

**實際成功率計算**：

**Stage 1（CT=1.0）**：
```
普通玩家：
f_attention = 1 / (1 + 0.08×1.0) = 0.926
P_counter = 0.15 × 0.926 = 0.139 (13.9%)

中階玩家：
f_attention = 1 / (1 + 0.04×1.0) = 0.962
P_counter = 0.40 × 0.962 = 0.385 (38.5%)

高手玩家：
f_attention = 1 / (1 + 0.02×1.0) = 0.980
P_counter = 0.75 × 0.980 = 0.735 (73.5%)
```

**Stage 20（CT=22.2）**：
```
普通玩家：
f_attention = 1 / (1 + 0.08×22.2) = 0.361
P_counter = 0.15 × 0.361 = 0.054 (5.4%)

中階玩家：
f_attention = 1 / (1 + 0.04×22.2) = 0.530
P_counter = 0.40 × 0.530 = 0.212 (21.2%)

高手玩家：
f_attention = 1 / (1 + 0.02×22.2) = 0.693
P_counter = 0.75 × 0.693 = 0.520 (52.0%)
```

### 反擊傷害貢獻（分層）

```
DMG_counter_contribution(tier, n) = P_counter(tier, n) × λ_pieces(tier) × N_counter(L_counter) × DMG_counter_single(tier)

其中：
N_counter = L_counter（反擊導彈數）
DMG_counter_single = 2.0 + E[C](tier) × L_burst × 0.25
```

**Stage 10貢獻計算**（L_counter=4, L_burst=3）：

```
普通玩家（C=4.5）：
DMG_counter_single = 2.0 + 4.5×3×0.25 = 5.375
貢獻 = 0.186 × 0.35 × 4 × 5.375 = 1.40 傷害/秒

中階玩家（C=10）：
DMG_counter_single = 2.0 + 10×3×0.25 = 9.5
貢獻 = 0.314 × 0.50 × 4 × 9.5 = 5.97 傷害/秒

高手玩家（C=30）：
DMG_counter_single = 2.0 + 30×3×0.25 = 24.5
貢獻 = 0.598 × 0.70 × 4 × 24.5 = 41.0 傷害/秒
```

**反擊貢獻佔比**（相對總DPS）：
```
普通玩家：1.40 / 50 = 2.8%
中階玩家：5.97 / 150 = 4.0%
高手玩家：41.0 / 450 = 9.1%

結論：高手玩家更能發揮反擊系統優勢
```

---

## 🎲 分層版面管理

### 板面穩定性（分層）

#### SP計算修正

```
SP(t, tier) = SP_space(t) × SP_defense(t, tier) × SP_void(t, tier)

其中SP_defense和SP_void受玩家能力影響
```

**空間管理能力**：
```
SP_space_effective(tier) = SP_space_raw × f_manage(tier)

f_manage_casual = 0.75（經常讓方塊堆積過高）
f_manage_inter = 0.90（較好控制）
f_manage_expert = 1.00（完美控制）
```

**虛無方塊處理能力**：
```
n_void_effective(tier) = n_void_actual × [1 - P_avoid(tier)]

P_avoid_casual = 0.10（10%概率避免虛無方塊）
P_avoid_inter = 0.30（30%）
P_avoid_expert = 0.50（50%）
```

**實際SP計算**（Stage 15，h_max=8, n_void=20, n_total=140）：

```
普通玩家：
SP_space = (20-8)/20 × 0.75 = 0.45
SP_defense = 0.60（假設L_defense=3）
SP_void = 1 - (20×0.9)/140 = 1 - 0.129 = 0.871
SP = 0.45 × 0.60 × 0.871 = 0.235（危險）

中階玩家：
SP_space = (20-8)/20 × 0.90 = 0.54
SP_defense = 0.70
SP_void = 1 - (20×0.7)/140 = 1 - 0.100 = 0.900
SP = 0.54 × 0.70 × 0.900 = 0.340（警戒）

高手玩家：
SP_space = (20-8)/20 × 1.00 = 0.60
SP_defense = 0.80
SP_void = 1 - (20×0.5)/140 = 1 - 0.071 = 0.929
SP = 0.60 × 0.80 × 0.929 = 0.446（警戒）
```

### 溢出風險（分層）

```
P_overflow(tier, n) = 1 - SP(tier, n) × [1 - α_overflow × CT(n)]

其中：
α_overflow: 威脅轉化為溢出的係數
```

**Stage 15溢出概率**：
```
普通玩家：
P_overflow = 1 - 0.235 × (1 - 0.05×9.86) = 1 - 0.120 = 0.880 (88%!)

中階玩家：
P_overflow = 1 - 0.340 × (1 - 0.05×9.86) = 1 - 0.172 = 0.828 (83%)

高手玩家：
P_overflow = 1 - 0.446 × (1 - 0.05×9.86) = 1 - 0.226 = 0.774 (77%)
```

**結論**：後期關卡對所有玩家都有高溢出風險，但差距明顯。

---

## 📈 火力曲線匹配分析

### PDA分層計算

#### 玩家傷害可用性（分層）

```
PDA(n, tier, L_buffs) = λ_clear(tier) × E[DMG_total](n, tier, L_buffs)

其中：
λ_clear(tier) = λ_pieces(tier) / E[r](tier)
```

**各關卡PDA計算**：

| Stage | HP | 普通PDA | 中階PDA | 高手PDA |
|-------|-----|---------|---------|---------|
| 1 | 120 | 11 | 33 | 74 |
| 2 | 160 | 15 | 42 | 95 |
| 3 | 240 | 20 | 56 | 135 |
| 4 | 300 | 26 | 75 | 180 |
| 5 | 400 | 49 | 150 | 390 |
| 6 | 500 | 58 | 180 | 480 |
| 7 | 600 | 68 | 215 | 590 |
| 8 | 700 | 78 | 250 | 710 |
| 9 | 900 | 92 | 295 | 880 |
| 10 | 1000 | 69 | 220 | 580 |
| 11 | 1200 | 85 | 280 | 750 |
| 12 | 1400 | 102 | 345 | 940 |
| 13 | 1600 | 122 | 420 | 1,150 |
| 14 | 1800 | 145 | 505 | 1,400 |
| 15 | 2000 | 133 | 475 | 1,350 |
| 16 | 2200 | 156 | 560 | 1,620 |
| 17 | 2600 | 185 | 680 | 2,000 |
| 18 | 3000 | 218 | 805 | 2,400 |
| 19 | 3600 | 264 | 1,000 | 3,050 |
| 20 | 5000 | 235 | 805 | 2,240 |

**注**：Stage 10, 15, 20的PDA下降是因為假設玩家在Boss關卡前剛升級Buff，效率暫時降低。

### 理論擊殺時間（分層）

```
T_kill(n, tier) = HP(n) / PDA(n, tier)
```

| Stage | HP | 普通T_kill | 中階T_kill | 高手T_kill | 理想範圍 |
|-------|-----|-----------|-----------|-----------|---------|
| 1 | 120 | 10.9s | 3.6s | 1.6s | 20-40s |
| 5 | 400 | 8.2s | 2.7s | 1.0s | 20-40s |
| 10 | 1000 | 14.5s | 4.5s | 1.7s | 20-40s |
| 15 | 2000 | 15.0s | 4.2s | 1.5s | 20-40s |
| 20 | 5000 | 21.3s | 6.2s | 2.2s | 20-40s |

**實際時間**（考慮overhead）：
```
T_actual(tier) = T_kill × (1 + overhead(tier))

overhead_casual = 0.80（80%額外時間）
overhead_inter = 0.40（40%）
overhead_expert = 0.20（20%）
```

| Stage | 普通T_actual | 中階T_actual | 高手T_actual | 評估 |
|-------|-------------|-------------|-------------|------|
| 1 | 19.6s | 5.0s | 1.9s | 普✓ 中✗ 高✗ |
| 5 | 14.8s | 3.8s | 1.2s | 普✓ 中✗ 高✗ |
| 10 | 26.1s | 6.3s | 2.0s | 普✓ 中✗ 高✗ |
| 15 | 27.0s | 5.9s | 1.8s | 普✓ 中✗ 高✗ |
| 20 | 38.3s | 8.7s | 2.6s | 普✓ 中✗ 高✗ |

**結論**：
- ✅ 普通玩家：時間合理（15-40秒）
- ✗ 中階玩家：過快（4-9秒）
- ✗ 高手玩家：極快（2-3秒）

### 難度指數匹配分析

#### 平衡條件

```
理想平衡：PDA(n, tier) × T_ideal = HP(n)

其中T_ideal = 25秒（目標戰鬥時長）
```

**所需PDA計算**：

| Stage | HP | 所需PDA | 普通PDA | 中階PDA | 高手PDA |
|-------|-----|---------|---------|---------|---------|
| 1 | 120 | 4.8 | 11 | 33 | 74 |
| 5 | 400 | 16 | 49 | 150 | 390 |
| 10 | 1000 | 40 | 69 | 220 | 580 |
| 15 | 2000 | 80 | 133 | 475 | 1,350 |
| 20 | 5000 | 200 | 235 | 805 | 2,240 |

**火力過剩比率**：
```
Excess_ratio(n, tier) = PDA(n, tier) / PDA_required(n)
```

| Stage | 普通比率 | 中階比率 | 高手比率 |
|-------|---------|---------|---------|
| 1 | 2.3× | 6.9× | 15.4× |
| 5 | 3.1× | 9.4× | 24.4× |
| 10 | 1.7× | 5.5× | 14.5× |
| 15 | 1.7× | 5.9× | 16.9× |
| 20 | 1.2× | 4.0× | 11.2× |

**趨勢分析**：
```
1. 普通玩家：Stage 20接近平衡（1.2×），前期偏易
2. 中階玩家：全關卡過於簡單（4-9倍火力過剩）
3. 高手玩家：嚴重失衡（11-24倍火力過剩）
```

### 分層平衡建議

#### 建議1：降低Burst倍率（分層）

**當前**：`BURST_DAMAGE_MULTIPLIER = 0.25`（統一）

**建議**：使用動態倍率
```
k_burst(C) = 0.25 × f_diminish(C)

f_diminish(C) = 1 / (1 + 0.005 × max(0, C - 10))

計算：
C=5:  f=1.00 → k_burst=0.25（無影響）
C=10: f=1.00 → k_burst=0.25（無影響）
C=20: f=0.95 → k_burst=0.238（-5%）
C=30: f=0.91 → k_burst=0.227（-9%）
C=40: f=0.87 → k_burst=0.217（-13%）
```

**效果**：
- 普通玩家：無影響
- 中階玩家：-2%傷害
- 高手玩家：-9%傷害（顯著）

#### 建議2：調整敵人HP（分層難度）

**當前**：統一HP曲線

**建議**：根據目標玩家層級設計不同模式

**模式A：休閒模式（針對普通玩家）**
```
HP_casual(n) = HP_current(n) × 0.6
目標：T_actual ∈ [15, 30]秒
```

**模式B：標準模式（針對中階玩家）**
```
HP_standard(n) = HP_current(n) × 1.8
目標：T_actual ∈ [20, 40]秒
```

**模式C：專家模式（針對高手玩家）**
```
HP_expert(n) = HP_current(n) × 4.5
目標：T_actual ∈ [20, 45]秒
```

**調整後預期時間**（Stage 20）：

| 模式 | HP | 玩家層級 | PDA | T_kill | T_actual |
|------|-----|---------|-----|--------|---------|
| 休閒 | 3000 | 普通 | 235 | 12.8s | 23.0s ✓ |
| 標準 | 9000 | 中階 | 805 | 11.2s | 15.7s ✓ |
| 專家 | 22500 | 高手 | 2240 | 10.0s | 12.0s ✗ |

**專家模式需進一步調整**：
```
HP_expert(20) = 35000（7倍當前值）
T_actual = 35000 / 2240 × 1.2 = 18.8s ✓
```

#### 建議3：Combo維持難度調整

**當前**：`COMBO_RESET_DELAY = 0.3s`（統一）

**建議**：根據當前Combo動態調整
```
t_reset(C) = t_base + Δt_bonus(C)

t_base = 0.3s
Δt_bonus(C) = min(0.3, 0.02 × C)

計算：
C=5:  t_reset=0.30+0.10=0.40s
C=10: t_reset=0.30+0.20=0.50s
C=20: t_reset=0.30+0.30=0.60s（上限）
C=30: t_reset=0.30+0.30=0.60s（上限）
```

**效果**：
- 低Combo：輕微幫助（+0.1s）
- 中Combo：明顯幫助（+0.2s）
- 高Combo：難度增加（上限鎖定）

**對各層級影響**：
```
普通玩家（C≈5）：略微受益
中階玩家（C≈10）：明顯受益
高手玩家（C≈30）：無額外受益，但也不受懲罰
```

---

## 📊 綜合匹配矩陣

### 當前平衡狀態

| Stage | DI | 普通 | 中階 | 高手 |
|-------|-----|------|------|------|
| 1 | 1.0 | ⚖️ 平衡 | 😊 過易 | 😊 極易 |
| 5 | 5.8 | ⚖️ 平衡 | 😊 過易 | 😊 極易 |
| 10 | 21.1 | ⚠️ 偏難 | ⚖️ 平衡 | 😊 極易 |
| 15 | 63.4 | ⚠️ 偏難 | ⚖️ 平衡 | 😊 極易 |
| 20 | 277.8 | ⚖️ 平衡 | 😊 過易 | 😊 極易 |

### 調整後預期平衡

**應用建議1+2+3後**：

| Stage | 模式 | 普通 | 中階 | 高手 |
|-------|------|------|------|------|
| 1 | 休閒 | ⚖️ 良好 | - | - |
| 1 | 標準 | - | ⚖️ 良好 | - |
| 1 | 專家 | - | - | ⚖️ 良好 |
| 10 | 休閒 | ⚖️ 良好 | - | - |
| 10 | 標準 | - | ⚖️ 良好 | - |
| 10 | 專家 | - | - | ⚖️ 良好 |
| 20 | 休閒 | ⚖️ 良好 | - | - |
| 20 | 標準 | - | ⚖️ 良好 | - |
| 20 | 專家 | - | - | ⚖️ 良好 |

**圖例**：
- ⚖️ 平衡/良好：T_actual ∈ [15, 40]秒
- ⚠️ 偏難：T_actual > 40秒
- 😊 過易：T_actual < 15秒

---

## 📚 交叉引用

**擴展自**：
- ← [01_Core_Variables.md](01_Core_Variables.md) - 基礎變量
- ← [02_Combat_Formulas.md](02_Combat_Formulas.md) - 傷害公式
- ← [04_Difficulty_Model.md](04_Difficulty_Model.md) - PDA和SP模型
- ← [05_Player_Model.md](05_Player_Model.md) - HOM模型

**應用於**：
- → [08_Tier_Balanced_Tuning.md](08_Tier_Balanced_Tuning.md) - 分層平衡調整（待創建）

**代碼影響**：
- `GameConstants.cs`: 需要動態Burst倍率
- `PlayerManager.cs`: 需要動態Combo重置時間
- `StageDataSO`: 需要難度模式配置
- `GameManager.cs`: 需要難度選擇系統

---

**文檔狀態**: ✅ 完整  
**新增模型**: ⭐ 三層技能模型  
**平衡分析**: ✅ 完整匹配矩陣  
**實施難度**: ⚠️ 中高（需要代碼重構）

